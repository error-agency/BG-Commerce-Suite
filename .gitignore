# Dependencies
node_modules/
/vendor/

# Release + verification artefacts (built from source, never committed)
*.zip
*.tar
*.tar.gz
sha256.txt
/build/
/dist/
/release/

# Secrets — never commit any of these.
# The staging SSH key lives in ~/.ssh, outside this repository, and neither the
# private nor the public half belongs here.
*.pem
*.key
*.ppk
id_rsa*
id_ed25519*
*_ed25519
*_ed25519.pub
*_rsa
*_rsa.pub
known_hosts
.ssh/
.env
.env.*
!.env.example
credentials*
secrets*
*.credentials
wp-config.php
auth.json

# Temporary / scratch
/tmp/
/temp/
/scratch/
/scratchpad/
*.log
*.bak
*.orig
*.rej
*~
.phpunit.result.cache

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.sublime-*

# Kept out of this repository on purpose
# dist/ ships as GitHub release assets; docs/ holds internal working notes and
# third-party courier manuals that are not ours to redistribute.
/docs/
/audit/
/.superpowers/
/tmp/
/docs.zip
/boxnow-webhook-test.ps1
BGCS-HANDOFF-*.md
BGCS-COURIER-PHASE-*-HANDOFF.md
BGCS-COURIER-PHASE-*-TEST-REPORT.md
BGCS-COURIER-PHASE-*-DIAGNOSTICS-REPORT.md
BGCS-MASTER-COURIER-AUDIT-AND-IMPROVEMENT-PLAN.md
